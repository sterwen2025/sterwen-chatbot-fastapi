# ==============================================================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# ==============================================================================
# API Configuration for production deployment

# Backend API URL - Since frontend and backend are deployed together,
# use relative path (nginx routes /api/* to backend)
VITE_API_URL=
